# SQLite Migration Archive

This folder stores historical SQLite Drizzle migrations and snapshots from the pre-PostgreSQL Ixoye schema.

- These files are kept for historical/reference use only.
- Active migrations now live in `drizzle/` and target PostgreSQL.
- Do not run archived SQLite migrations in current environments.
